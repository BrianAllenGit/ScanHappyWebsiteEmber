
import { formatEpoch } from 'scan-happy-website/helpers/format-epoch';
import { module, test } from 'qunit';

module('Unit | Helper | format epoch');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatEpoch([42]);
  assert.ok(result);
});

